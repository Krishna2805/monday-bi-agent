# normalizer/ — Data cleaning and transformation layer.
# Handles Excel date conversion, placeholder amount detection, quantity parsing,
# embedded header row removal, and master normalization functions.
# The analytics layer NEVER sees raw Monday.com data — only normalized dicts from here.
