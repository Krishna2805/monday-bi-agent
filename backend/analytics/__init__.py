# analytics/ — Pure Python business calculation functions.
# Pipeline metrics (weighted value, funnel, win rate) and revenue metrics
# (billed, collected, AR, unbilled). These are standalone, deterministic,
# and 100% unit-testable — they take normalized dicts in, return summary dicts out.
