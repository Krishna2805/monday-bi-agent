# Monday.com Business Intelligence Agent
# Python Dependencies

# --- Web Framework ---
fastapi==0.115.12          # Async web framework for the API layer
uvicorn[standard]==0.34.3  # ASGI server to run FastAPI

# --- HTTP Client ---
httpx==0.28.1              # Async HTTP client for Monday.com GraphQL API calls

# --- Google Gemini ---
google-genai==1.3.0        # Official new Google GenAI SDK (supports Gemini 2.5/3.0/Flash & function calling)

# --- Data Processing ---
pandas==2.2.3              # DataFrame operations for normalization and aggregation

# --- Data Validation ---
pydantic==2.11.4           # Request/response model validation (used by FastAPI)

# --- Environment ---
python-dotenv==1.1.0       # Load .env files into os.environ

# --- Caching (Phase 8) ---
cachetools==5.5.2          # Simple TTL cache for Monday.com API responses

# --- CORS ---
# (included in FastAPI via starlette — no extra install needed)
